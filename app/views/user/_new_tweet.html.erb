<div class="tweet-form">
  <%= form_for tweet, url: create_tweet_url do |f| %>
  <br>
  <%= f.text_area :message, autofocus: true, cols: 100, rows: 6 %>
  <br>
  <%= f.submit "Tweet It!", class: "btn btn-primary" %>
  <% end %>
</div>
